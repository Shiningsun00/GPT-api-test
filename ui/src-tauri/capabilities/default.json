{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default local desktop capability. The frontend does not receive shell execution permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
